<?xml version="1.0" encoding="utf-8" ?>
<base:SfContentPage
    x:Class="Mde.Project.Mobile.Pages.Authentication.ResetPasswordPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:base="clr-namespace:Mde.Project.Mobile.Controls.Base"
    xmlns:fa="clr-namespace:UraniumUI.Icons.FontAwesome;assembly=UraniumUI.Icons.FontAwesome"
    xmlns:input="clr-namespace:InputKit.Shared.Controls;assembly=InputKit.Maui"
    xmlns:m="clr-namespace:UraniumUI.Icons.MaterialIcons;assembly=UraniumUI.Icons.MaterialIcons"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:validation="clr-namespace:InputKit.Shared.Validations;assembly=InputKit.Maui"
    xmlns:vm="clr-namespace:Mde.Project.Mobile.ViewModels.Authentication"
    x:DataType="vm:ResetPasswordViewModel"
    NavigationPage.HasNavigationBar="False">
    <Grid>
        <Image Aspect="AspectFill" Source="subtle_prism.png" />
        <ScrollView VerticalOptions="Fill">
            <Grid MaximumWidthRequest="600" RowDefinitions="300, *">
                <Grid
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    RowDefinitions=".6*, Auto"
                    VerticalOptions="Center">
                    <Image
                        Margin="15,0,0,0"
                        HorizontalOptions="Center"
                        MaximumHeightRequest="{OnPlatform Android=150,
                                                          WinUI=200}"
                        Source="logo_stock.png" />
                    <Label
                        Grid.Row="2"
                        Margin="0,-10,0,20"
                        FontAttributes="Bold"
                        FontFamily="bold"
                        FontSize="25"
                        HorizontalOptions="Center"
                        Text="Stock Flow"
                        TextColor="{StaticResource Gray200}" />
                </Grid>
                <Grid Grid.Row="1" StyleClass="Elevation3">
                    <RoundRectangle
                        Margin="-5,-30,-5,-2"
                        CornerRadius="30,30,0,0"
                        Fill="{AppThemeBinding Light={StaticResource LightBackground},
                                               Dark={StaticResource Jet}}" />
                    <VerticalStackLayout Margin="30,0,30,0">
                        <Label
                            FontAttributes="Bold"
                            FontFamily="bold"
                            FontSize="20"
                            Text="New Password"
                            TextColor="{AppThemeBinding Light={StaticResource Jet},
                                                        Dark={StaticResource GhostWhite}}" />
                        <HorizontalStackLayout>
                            <Label
                                FontSize="14"
                                Text="Create your new password"
                                TextColor="{AppThemeBinding Light={StaticResource Jet},
                                                            Dark={StaticResource GhostWhite}}" />
                        </HorizontalStackLayout>
                        <input:FormView SubmitCommand="{Binding ResetPasswordCommand}">
                            <material:TextField
                                Title="New Password"
                                Margin="0,20,0,0"
                                AccentColor="{AppThemeBinding Light={StaticResource Cerulean},
                                                              Dark={StaticResource BlueMunself}}"
                                Icon="{FontImageSource FontFamily=FASolid,
                                                       Glyph={x:Static fa:Solid.Lock}}"
                                IsPassword="True"
                                Text="{Binding Password}">
                                <validation:RequiredValidation />
                                <validation:MinLengthValidation Message="Requires a minimum length of 5" MinLength="5" />
                            </material:TextField>
                            <material:TextField
                                Title="Repeat Password"
                                Margin="0,10,0,20"
                                AccentColor="{AppThemeBinding Light={StaticResource Cerulean},
                                                              Dark={StaticResource BlueMunself}}"
                                Icon="{FontImageSource FontFamily=FASolid,
                                                       Glyph={x:Static fa:Solid.Lock}}"
                                IsPassword="True"
                                Text="{Binding RepeatPassword}">
                                <validation:RequiredValidation />
                                <validation:MinLengthValidation Message="Requires a minimum length of 5" MinLength="5" />
                            </material:TextField>
                            <ActivityIndicator
                                Margin="0,0,0,20"
                                HorizontalOptions="Center"
                                IsRunning="{Binding ResetPasswordCommand.IsRunning}"
                                IsVisible="{Binding ResetPasswordCommand.IsRunning}"
                                VerticalOptions="Center" />
                            <Button
                                input:FormView.IsSubmitButton="True"
                                BackgroundColor="{StaticResource Charcoal}"
                                IsEnabled="{Binding ResetPasswordCommand.IsRunning, Converter={toolkit:InvertedBoolConverter}}"
                                StyleClass="FilledButton"
                                Text="Confirm"
                                TextColor="{StaticResource GhostWhite}" />
                        </input:FormView>
                    </VerticalStackLayout>
                </Grid>
            </Grid>
        </ScrollView>
    </Grid>
</base:SfContentPage>